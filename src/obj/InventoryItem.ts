// Define a simple interface for your item data for clarity
export interface InventoryItem {
    itemType: string;
    textureKey: string;
    frame: number;
    quantity: number;
    // Add any other properties like 'value', 'description', 'effect' here
}
